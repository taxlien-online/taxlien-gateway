# ===========================================
# taxlien-gateway — Environment Configuration
# ===========================================
# Copy to .env and set values per environment.
# Used by docker-compose for CI/CD and local runs.

# --- Core (isolates containers/networks per environment) ---
COMPOSE_PROJECT_NAME=taxlien-gateway-dev
ENV_TAG=dev

# --- Network: bind to specific IP to avoid conflicts with other projects ---
BIND_IP=0.0.0.0

# --- External volumes: DB and raw data on host (must exist) ---
DATA_DIR=./data

# --- Ports ---
GATEWAY_PORT=8081
DB_PORT=5432
REDIS_PORT=6379
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000

# --- Secrets (set on server, do not commit) ---
GATEWAY_WORKER_TOKENS=
GRAFANA_PASSWORD=admin

# ===========================================
# Examples:
# ===========================================
#
# Linux production (dedicated IP):
#   COMPOSE_PROJECT_NAME=taxlien-gateway-prod
#   ENV_TAG=prod
#   BIND_IP=10.0.0.1
#   DATA_DIR=/opt/taxlien-gateway/prod/data
#
# Linux development:
#   COMPOSE_PROJECT_NAME=taxlien-gateway-dev
#   ENV_TAG=dev
#   BIND_IP=10.0.0.2
#   DATA_DIR=/opt/taxlien-gateway/dev/data
#
# macOS stage (localhost):
#   COMPOSE_PROJECT_NAME=taxlien-gateway-stage
#   ENV_TAG=stage
#   BIND_IP=127.0.0.1
#   DATA_DIR=./data
